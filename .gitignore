# Generated by running the demos/tests -- not source, don't ship these.
learning_memory.json
learned_policy.json
multiseed_metrics.json
repair_decisions.jsonl
workflow_dashboard.html
llm_judge_cache.json
/tmp/multiseed_memory_seed*.json

# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# A small, explicit "here's what a run produces" sample belongs under
# examples/ instead, committed on purpose -- not as the working-directory
# output of every test/demo run.
